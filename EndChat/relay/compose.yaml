services:
  relay:
    build: .
    restart: unless-stopped
    environment:
      RELAY_TOKEN: ${RELAY_TOKEN:?set RELAY_TOKEN in .env}
      DATA_DIR: /data
    ports:
      - "127.0.0.1:8080:8080"
    volumes:
      - endchat-relay-data:/data

volumes:
  endchat-relay-data:
